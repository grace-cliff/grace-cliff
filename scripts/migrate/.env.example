SANITY_PROJECT_ID=
SANITY_DATASET=production
# Generate at https://www.sanity.io/manage -> your project -> API -> Tokens.
# Needs "Editor" (write) access. Delete the token again once the migration
# has run — it's only needed for this one-time script.
SANITY_TOKEN=
